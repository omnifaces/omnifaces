{
  "name": "omnifaces",
  "version": "5.0",
  "description": "Build omnifaces.js",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/omnifaces/omnifaces.git"
  },
  "scripts": {
    "build": "esbuild src/OmniFaces.ts --bundle --format=iife --global-name=OmniFaces --target=es2020 --minify --outfile=../../../target/classes/META-INF/resources/omnifaces/omnifaces.js",
    "lint": "eslint .",
    "lint:fix": "eslint --fix ."
  },
  "devDependencies": {
    "@stylistic/eslint-plugin": "^4.2.0",
    "esbuild": "^0.27.4",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.57.2"
  }
}
