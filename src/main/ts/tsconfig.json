{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ES2020",
    "moduleResolution": "bundler",
    // Do not set strict here; esbuild would emit "use strict" into the bundle. TypeScript 6 checks strictly by default.
    "noEmit": true
  },
  "files": [
    "src/OmniFaces.ts"
  ]
}
